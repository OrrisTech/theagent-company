import { Radio } from "lucide-react";
import { PlaceholderPage } from "../../components/PlaceholderPage";

export function ChannelsSettings() {
  return (
    <PlaceholderPage
      titleKey="pages.channels.title"
      descriptionKey="pages.channels.description"
      icon={Radio}
    />
  );
}
