import { Workflow } from "lucide-react";
import { PlaceholderPage } from "../components/PlaceholderPage";

export function Workflows() {
  return (
    <PlaceholderPage
      titleKey="pages.workflows.title"
      descriptionKey="pages.workflows.description"
      icon={Workflow}
    />
  );
}
